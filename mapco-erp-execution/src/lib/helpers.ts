export function formatNumber(value:number):string{ return new Intl.NumberFormat('en-US',{minimumFractionDigits:0,maximumFractionDigits:2}).format(Number(value||0)); }
export function formatCurrency(value:number):string{ return formatNumber(value); }
export function todayISO(){ return new Date().toISOString().slice(0,10); }
export function formatDate(value?:string):string{ if(!value) return '-'; return value; }
export function statusTone(status:string):string{ const map:Record<string,string>={'قيد الشحن':'bg-amber-100 text-amber-700','في انتظار التحصيل':'bg-orange-100 text-orange-700','وصلت':'bg-emerald-100 text-emerald-700','تحت التخليص':'bg-sky-100 text-sky-700','جاري التصميم':'bg-violet-100 text-violet-700','تحت التصنيع':'bg-sky-100 text-sky-700','جاهز للاستلام':'bg-emerald-100 text-emerald-700','مكتمل':'bg-emerald-100 text-emerald-700','ناقص':'bg-rose-100 text-rose-700','قيد المراجعة':'bg-amber-100 text-amber-700','نشط':'bg-emerald-100 text-emerald-700','تحت الصيانة':'bg-amber-100 text-amber-700','مؤرشف':'bg-slate-100 text-slate-700'}; return map[status] ?? 'bg-slate-100 text-slate-700';}
export function calculateShipmentTotals(shipmentValue:number,shippingCost:number,customsRate=0.075,vatRate=0.15){ const customsBase=Number(shipmentValue||0)+Number(shippingCost||0); const customs=Number((customsBase*Number(customsRate||0)).toFixed(2)); const vat=Number(((customsBase+customs)*Number(vatRate||0)).toFixed(2)); const total=Number((customsBase+customs+vat).toFixed(2)); return {customs,vat,total}; }
export function downloadTextFile(name:string,content:string):string{ const blob=new Blob([content],{type:'text/csv;charset=utf-8;'}); const url=URL.createObjectURL(blob); const a=document.createElement('a'); a.href=url; a.download=name; a.click(); URL.revokeObjectURL(url); return name; }
export const requiredDieMaterialTypes=['خشب','سكين','ربر'];
